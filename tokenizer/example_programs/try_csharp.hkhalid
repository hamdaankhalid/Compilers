public Test {
  private Logger logger;

  public Test(Logger logger) {
    this.logger = logger;
  }

  public void Run() {
    this.logger = logger;
    int i = 0;
    while(i <= 5) {
      this.logger.LogInformation("its me mario");
    }
  }
}
